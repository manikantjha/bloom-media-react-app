import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FBE555",
    },
    secondary: {
      main: "#232020",
      light: "#424242",
    },
  },
});

export default theme;
