import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#f26b37",
    },
    secondary: {
      main: "#414042",
    },
  },
});

export default theme;
