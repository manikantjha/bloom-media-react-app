import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FBE555",
    },
    secondary: {
      main: "#232020",
      light: "#3A3535",
    },
  },
});

export default theme;
